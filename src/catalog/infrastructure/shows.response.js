/**
 * @summary Response type for the TVMaze shows endpoint.
 * @author Josue Antonio Flores Apaico (u20201f773)
 *
 * @remarks
 * The TVMaze /shows endpoint returns a plain JSON array of show resources
 * (no status envelope), wrapped by Axios in the response data property.
 */

/**
 * API response structure for the shows endpoint.
 *
 * @typedef {import('./show.resource.js').ShowResource[]} ShowsResponse
 */

export {};
